# Optional. The device catalog is read from the sibling growrig-platform repo,
# falling back to the bundled snapshot in source/ when it is not present.
GROWRIG_DEVICES_DIR=../growrig-platform/devices
SITE_URL=https://growrig.dev
BASE_PATH=/
